version = "1.2.1"
description = "Adds an array of new channel icons"

aliucord.changelog.set(
    """
    Fixes {fixed marginTop}
    ======================
    
    * Small fixes
    """.trimIndent()
)